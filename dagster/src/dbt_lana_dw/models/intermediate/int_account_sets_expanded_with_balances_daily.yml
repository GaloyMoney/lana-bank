version: 2

models:
  - name: int_account_sets_expanded_with_balances_daily
    columns:
      - name: as_of_date
        tests:
          - not_null
      - name: account_set_id
        tests:
          - not_null
      - name: member_id
        tests:
          - not_null
      - name: member_type
        tests:
          - accepted_values:
              arguments:
                values: ["Account"]
