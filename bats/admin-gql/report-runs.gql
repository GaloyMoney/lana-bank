query ReportRuns($first: Int!) {
  reportRuns(first: $first) {
    nodes {
      reportRunId
      state
      requestedAsOfDate
      requestedReport {
        reportDefinitionId
      }
      reports {
        reportId
        files {
          extension
        }
      }
    }
  }
}
