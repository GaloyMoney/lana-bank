name: Deploy docs on release

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "lana-bank-docs"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'promote-rc')
    runs-on: ubuntu-latest
    environment:
      name: lana-bank-docs
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v4

      - name: Run the Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v8

      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: true

      - name: Install dependencies
        run: |
          nix develop -c bash -lc "cd docs-site && npm ci"

      - name: Generate API docs (current version)
        run: |
          nix develop -c bash -lc "cd docs-site && npm run generate-api-docs"

      - name: Generate API docs (versioned)
        run: |
          nix develop -c bash -lc "cd docs-site && npm run generate-versioned-api-docs"

      - name: Build docs site
        run: |
          nix develop -c bash -lc "cd docs-site && npx docusaurus build"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs-site/build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
