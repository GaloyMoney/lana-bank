---
id: loan-agreement-generate
title: loanAgreementGenerate
---
import TryInSandbox from '@site/src/components/TryInSandbox';





export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Genera un documento de contrato de prestamo para un cliente

<TryInSandbox
  endpoint="http://admin.localhost:4455/graphql"
  operationName="loanAgreementGenerate"
  operation={`mutation LoanAgreementGenerate(\$input: LoanAgreementGenerateInput!) {
  loanAgreementGenerate(input: \$input) {
    # Add fields here
    __typename
  }
}`}
/>

```graphql
loanAgreementGenerate(
  input: LoanAgreementGenerateInput!
): LoanAgreementGeneratePayload!
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>loanAgreementGenerate.<b>input</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>LoanAgreementGenerateInput!</b></code>](/api/admin/types/inputs/loan-agreement-generate-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/> 



### Type

#### [<code style={{ fontWeight: 'normal' }}><b>LoanAgreementGeneratePayload</b></code>](/api/admin/types/objects/loan-agreement-generate-payload.mdx) <Badge class="badge badge--secondary " text="object"/>