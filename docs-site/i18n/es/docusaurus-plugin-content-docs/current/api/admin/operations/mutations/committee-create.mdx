---
id: committee-create
title: committeeCreate
---
import TryInSandbox from '@site/src/components/TryInSandbox';





export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Crea un nuevo comite de aprobacion

<TryInSandbox
  endpoint="http://admin.localhost:4455/graphql"
  operationName="committeeCreate"
  operation={`mutation CommitteeCreate(\$input: CommitteeCreateInput!) {
  committeeCreate(input: \$input) {
    # Add fields here
    __typename
  }
}`}
/>

```graphql
committeeCreate(
  input: CommitteeCreateInput!
): CommitteeCreatePayload!
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>committeeCreate.<b>input</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>CommitteeCreateInput!</b></code>](/api/admin/types/inputs/committee-create-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/> 



### Type

#### [<code style={{ fontWeight: 'normal' }}><b>CommitteeCreatePayload</b></code>](/api/admin/types/objects/committee-create-payload.mdx) <Badge class="badge badge--secondary " text="object"/>