---
id: descendant-account-sets-by-category
title: descendantAccountSetsByCategory
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Devuelve los grupos de cuentas descendientes (cuentas no terminales) bajo una categoria contable especificada (activo, pasivo, capital, ingresos, costo de ventas, gastos, o fuera de balance)

```graphql
descendantAccountSetsByCategory(
  category: AccountCategory!
): [AccountInfo!]!
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>descendantAccountSetsByCategory.<b>category</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>AccountCategory!</b></code>](/for-developers/admin-api/types/enums/account-category.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="enum"/> 



### Type

#### [<code style={{ fontWeight: 'normal' }}><b>AccountInfo</b></code>](/for-developers/admin-api/types/objects/account-info.mdx) <Badge class="badge badge--secondary " text="object"/>