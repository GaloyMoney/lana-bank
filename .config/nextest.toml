# core-price and core-credit share a PostgreSQL NOTIFY channel for ephemeral
# price events. core-credit's seed_price helper can leak into core-price's
# listener, causing flaky failures. Run core-price tests exclusively to
# prevent cross-package interference.

[[profile.default.overrides]]
filter = 'package(core-price)'
threads-required = "num-test-threads"
